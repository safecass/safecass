{
    "filter" : [
        // threshold filter
        {   // common fields
            "class_name"       : "FilterThreshold",
            "target_component" : "sensorWrapper",
            "type"             : "ACTIVE",
            // threshold filter specific fields
            "argument" : {
                "input_signal" : "ForceY",
                "threshold"    : 10.0,
                "margin"       : 0.5,
                "output0"      : 0.0,
                "output1"      : 1.0
            },
            // if this filter is the last in filter pipeline
            "last_filter" : true
        }
    ]
}
